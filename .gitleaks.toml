# gitleaks 配置：继承默认规则集，扫描所有已跟踪的公开内容。
# 真实凭据只存在于 SSM 参数库、GitHub secrets 或部署主机的 secrets/<env>/*.txt。
# 已确认的历史假凭据只通过 .gitleaksignore 的 commit-scoped fingerprint 放行。
# 禁止用 path 或宽泛 regex 跳过整个 docs、tests、work-items、模板或示例目录。

[extend]
useDefault = true
