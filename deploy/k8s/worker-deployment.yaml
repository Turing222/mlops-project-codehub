apiVersion: apps/v1
kind: Deployment
metadata:
  name: dewflow-worker
  namespace: dewflow
  labels:
    app.kubernetes.io/name: dewflow-worker
    app.kubernetes.io/component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dewflow-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dewflow-worker
        app.kubernetes.io/component: worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
      terminationGracePeriodSeconds: 130
      containers:
        - name: worker
          image: dewflow-backend:2.0.0-ai
          imagePullPolicy: IfNotPresent
          # Task modules live in env so the image default CMD picks them up
          # (matches deploy/docker-compose.yml).
          env:
            - name: TASKIQ_HEALTH_MIN_PROCESSES
              value: "2"
            - name: TASKIQ_MODULES
              value: "backend.worker.tasks.llm_tasks backend.worker.tasks.knowledge_tasks backend.worker.tasks.repo_analysis_tasks backend.worker.tasks.credit_tasks"
            - name: TASKIQ_WORKERS
              value: "2"
            - name: TASKIQ_WAIT_TASKS_TIMEOUT
              value: "105"
            - name: TASKIQ_SHUTDOWN_TIMEOUT
              value: "10"
          envFrom:
            - configMapRef:
                name: dewflow-config
            - secretRef:
                name: dewflow-secret
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1536Mi
          livenessProbe:
            exec:
              command:
                - python
                - -m
                - backend.worker.tasks.healthcheck
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 2
          readinessProbe:
            exec:
              command:
                - python
                - -m
                - backend.worker.tasks.healthcheck
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 2
          volumeMounts:
            - name: knowledge-files
              mountPath: /data/knowledge_files
      volumes:
        - name: knowledge-files
          persistentVolumeClaim:
            claimName: dewflow-knowledge-files
